name: Actions Probe

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/actions-probe.yml"

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions is available"
